name: E2E Tests (Playwright)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  playwright:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root_password
          MYSQL_DATABASE: testdb
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -u root -p$$MYSQL_ROOT_PASSWORD"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@v4

      # 1️⃣ Setup Node.js
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # 2️⃣ Wait for MySQL service to become healthy
      - name: Wait for MySQL Service
        run: |
          for i in {1..10}; do
            if [[ "$(docker inspect --format='{{.State.Health.Status}}' ${{ job.services.mysql.id }})" == "healthy" ]]; then
              echo "✅ MySQL service is healthy. Waiting 5s for network stability..."
              sleep 5 # Critical: Wait an extra 5s for EAI_AGAIN fix
              exit 0
            fi
            echo "⏳ Waiting for MySQL service... Attempt $i"
            sleep 10
          done
          echo "❌ MySQL did not become healthy in time."
          exit 1
      
      # 3️⃣ Database Cleanup (Crucial fix for ER_DUP_ENTRY)
      - name: Clean Database Before Backend Start
        run: |
          echo "🧹 Dropping and recreating testdb..."
          mysql -h 127.0.0.1 -P 3306 -u root -proot_password -e "DROP DATABASE IF EXISTS testdb; CREATE DATABASE testdb;"

      # 4️⃣ Install dependencies
      - name: Install Dependencies
        run: |
          npm ci --prefix ./back-end/server
          npm ci --prefix ./front-end
          npm ci --prefix ./e2e-tests

      # 5️⃣ Install Playwright browsers (after npm ci)
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
        working-directory: ./e2e-tests

      # 6️⃣ Create .env.ci for Backend Configuration
      - name: Create .env.ci file
        run: |
          echo "PORT=5001" > back-end/server/.env.ci
          echo "DB_HOST=mysql" >> back-end/server/.env.ci
          echo "DB_USER=root" >> back-end/server/.env.ci
          echo "DB_PASSWORD=root_password" >> back-end/server/.env.ci
          echo "DB_NAME=testdb" >> back-end/server/.env.ci
          echo "JWT_SECRET=a_very_strong_and_secret_key_that_no_one_can_guess" >> back-end/server/.env.ci
        working-directory: ./

      # 7️⃣ Start Backend with logs
      - name: Start Backend Server
        run: |
          echo "🔹 Starting backend..."
          # Source the .env.ci file to set environment variables
          source back-end/server/.env.ci
          # Start the server and pipe output to a log file
          node back-end/server/server.js > back-end/server/server.log 2>&1 &
          echo "🔹 Backend PID: $!"
        working-directory: ./

      # 8️⃣ Wait for Backend HTTP Health Check (Robust)
      - name: Wait for Backend to be Ready (Health Check)
        run: |
          BACKEND_LOG=./back-end/server/server.log
          
          # Use curl against the new /health endpoint for a robust check
          timeout 180 bash -c 'until curl -s http://127.0.0.1:5001/health > /dev/null; do
            echo "⏳ Waiting for backend (5001/health)..."
            # Tail the log file to see connection errors/crashes
            tail -n 15 "$BACKEND_LOG"
            sleep 5
          done
          echo "✅ Backend is ready and responding!"'

      # 9️⃣ Start Frontend (Vite)
      - name: Start Frontend
        run: npm run dev --prefix front-end > front-end/vite.log 2>&1 &
      
      # 🔟 Wait for Frontend to be ready (HTTP Check)
      - name: Wait for Frontend
        run: |
          timeout 180 bash -c 'until curl -s http://127.0.0.1:5173 > /dev/null; do
            echo "⏳ Waiting for frontend..."
            sleep 5
          done
          echo "✅ Frontend is ready!"'

      # 11 Run Playwright E2E Tests
      - name: Run Playwright E2E Tests
        run: npx playwright test --reporter=html
        working-directory: ./e2e-tests

      # 12 Upload Playwright report as artifact
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: e2e-tests/playwright-report/
          retention-days: 30