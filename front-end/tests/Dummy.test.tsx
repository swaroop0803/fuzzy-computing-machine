import { it } from 'vitest'
import { expect,describe } from 'vitest'


describe('Dummy', () => {
    it('should show the output correctly', () => {
    expect(4+5).toBe(9)
    })
    it('should show the output correctly', () => {
    expect(4-5).toBe(-1)
    })
    it('should show the output correctly', () => {
    expect(4*5).toBe(20)
    })
    it('should show the output correctly', () => {
    expect(4/5).toBe(0.8)
    })
    
})